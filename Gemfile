source "https://rubygems.org"

ruby "~> 3.3"

# Match the GitHub Pages build stack for local previews.
group :jekyll_plugins do
  gem "github-pages", "~> 232"
end

# Ruby 3 no longer ships WEBrick, but Jekyll still uses it for `serve`.
gem "webrick", "~> 1.8"
